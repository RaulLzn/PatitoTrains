package patitotrains.controller.interfaces;
import java.net.URL;
import java.util.ResourceBundle;


/**
 * Interfaz que contiene los métodos de la vista de búsqueda de boletos
 */
public interface SearchTicketControllerInterface {
    /**
     * Método que inicializa la vista
     * @param arg0
     * @param arg1
     */
    void initialize(URL arg0, ResourceBundle arg1);
}
